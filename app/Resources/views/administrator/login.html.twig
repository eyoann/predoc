<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Page de connexion</title>
   
   <!-- BOOTSTRAP STYLES-->
    <link href={{ asset('bundles/App/css/bootstrap.css') }} rel="stylesheet" />

     <!-- FONTAWESOME STYLES-->
	 
    <link href={{ asset('bundles/App/css/font-awesome.css') }} rel="stylesheet" />
        <!-- CUSTOM STYLES-->
		
    <link href={{ asset('bundles/App/css/custom.css') }} rel="stylesheet" />
     <!-- GOOGLE FONTS-->
   <link href='http://fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css' />


</head>
<body>
    <div class="container">
        <div class="row text-center ">
            <div class="col-md-12">
                <br /><br />
                <h2>Page de connexion</h2>
               
                
				  {% if error %}
    <div class="alert alert-danger">nom d'utilisateur ou mot de passe incorrect</div>
  {% endif %}
                 <br />
            </div>
        </div>
         <div class="row ">
               
                  <div class="col-md-4 col-md-offset-4 col-sm-6 col-sm-offset-3 col-xs-10 col-xs-offset-1">
                        <div class="panel panel-default">
                            <div class="panel-heading">
                        <strong>   Login et mot de passe </strong>  
                            </div>
                            <div class="panel-body">
                                <form role="form" action="{{ path('fos_user_security_check') }}" method="post" >
                                <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                                       <br />
                                     <div class="form-group input-group">
                                            <span class="input-group-addon"><i class="fa fa-tag"  ></i></span>
											<input type="text" id="username" class="form-control" name="_username" value="{{ last_username }}" placeholder="Votre Username " />
                                        </div>
                                            <div class="form-group input-group">
                                            <span class="input-group-addon"><i class="fa fa-lock"  ></i></span>
                                           
											<input type="password" class="form-control" id="password" name="_password" placeholder="Votre Password" />
                                        </div>
                                     
									 <input type="submit" class="btn btn-primary " value="Connexion" />
                                     
                                    <hr />
                                 </form>
                            </div>
                           
                        </div>
                    </div>
                
                
        </div>
    </div>


     <!-- SCRIPTS -AT THE BOTOM TO REDUCE THE LOAD TIME-->

	     <!-- JQUERY SCRIPTS -->
    <script src={{ asset('bundles/App/js/jquery-1.10.2.js') }}></script>
      <!-- BOOTSTRAP SCRIPTS -->
    <script src={{ asset('bundles/App/js/bootstrap.min.js') }}></script>
    <!-- METISMENU SCRIPTS -->
    <script src={{ asset('bundles/App/js/jquery.metisMenu.js') }}></script>
      <!-- CUSTOM SCRIPTS -->
    <script src={{ asset('bundles/App/js/custom.js') }}></script>
   
</body>
</html>
