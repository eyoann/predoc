{% extends 'administrator/tempalte.html.twig' %}

{% block contenu %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="col-md-6">
    <div class="form-group">
        <div class="col-xs-12 text-right">
            <button class="btn btn-primary" type="submit"> Valider </button>
        </div>
    </div>
    </div>
    {{ form_end(form) }}
    {% endblock %}